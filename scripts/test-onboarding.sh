#!/bin/bash

echo "🚀 Testing Onboarding Flows"
echo ""
echo "Available test flows:"
echo "1. Retailer: welcome → role → retailor → otp (123456) → password → signup → login"
echo "2. Supplier: welcome → role → supplier → otp (123456) → password → signup → pending-approval"
echo "3. Agent: welcome → role → agent → otp (123456) → password → signup → pending-approval"
echo ""
echo "Mock OTP codes: 123456 or 000000"
echo ""
echo "Run: npm start or expo start"
